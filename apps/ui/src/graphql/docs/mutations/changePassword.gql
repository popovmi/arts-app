mutation changePassword(
  $username: String!
  $password: String!
  $passwordRepeat: String!
  $newPassword: String!
) {
  changePassword(
    username: $username
    password: $password
    passwordRepeat: $passwordRepeat
    newPassword: $newPassword
  )
}
