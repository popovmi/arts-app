mutation updateArtComment($id: Float!, $text: String!) {
    updateArtComment(id: $id, text: $text) {
        id
        text
		artId
        author {
            id
            fullName
        }
    }
}
