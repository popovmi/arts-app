mutation updateProjectComment($id: Float!, $text: String!) {
    updateProjectComment(id: $id, text: $text) {
        id
        text
        projectId
        author {
            id
            fullName
        }
    }
}
